# Asset Sources — Surf Haven

## WLFS identity (generated 2026-07-05)

- **Header/footer lockup**: `public/assets/img/logo/surf-haven/logo.png` and
  `logo_white.png` — cursive Webdevful `web` + vertical divider + "Surf Haven",
  transparent PNG, 500-class horizontal lockup rendered at the source logo
  slot's aspect; white variant on the dark hero/footer, dark variant swapped in
  by `initLogoSwap()` on the scrolled light navbar. Generated by the canonical
  `tools/generate_logos.py` (WLFS); Webdevful-owned identity output.
- **Browser-tab favicon bundle**: `favicon.png`, `favicon-32x32.png`,
  `favicon-16x16.png` — white cursive Webdevful `W` in the company-red square
  tab, linked from the page `<head>`; `favicon-tab.png` / `favicon-tab_white.png`
  kept as the horizontal `W | Surf Haven` tab-identity proof lockups.
  Browser proof: page `<title>` starts `Surf Haven |` so the tab reads
  W-square + name.

## Runtime + media

- **GSAP 3.15.0 + ScrollTrigger + SplitText**: vendored locally under
  `public/scripts/` at the exact versions the Webflow source loads
  (source-runtime parity; GSAP is free for all uses incl. commercial).
- **Swiper 11**: npm dependency + vendored bundle `public/scripts/`; drives
  every source `w-slider` via the Swiper adapter in `surf-haven.js`.
- **Video diaries player**: `public/assets/video/surf-diaries.mp4` — Pexels
  video #19955848 (Pexels license: free for commercial use, no attribution
  required), 1366x720, 17s; inline muted preview + click-to-play lightbox.
- **Photography**: source-template imagery retained pending the owner's
  provenance decision (flagged in the open repair order).
